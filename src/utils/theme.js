import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#375CF5'
    },
    secondary: {
      main: '#F69220',
      subMain: '#EA088B'
    },
    textColor: {
      main: '#202A50',
      subMain: '#AFB3C0'
    }
  }
})
